---
title: "六、分页与多条件查询"
---

# 六、分页与多条件查询

### 6.1 用 Specification 拼装动态条件

查询条件全部可选时，用 JPA 的 `Specification` 动态组装，比写一堆 `@Query` 方法名组合更清晰。

`service/ActivityService.java`

```java
@Transactional(readOnly = true)
public PageResult<ActivityResponse> search(Long venueId, ActivityType type,
                                           String keyword, int page, int size) {
    // 防御性收敛：即使有人传 size=100000，也不会把数据库拖垮
    int safeSize = Math.min(Math.max(size, 1), appProperties.maxPageSize());
    int safePage = Math.max(page, 0);

    Sort sort = Sort.by(Sort.Direction.DESC, "createdAt")
            .and(Sort.by(Sort.Direction.DESC, "id"));
    Pageable pageable = PageRequest.of(safePage, safeSize, sort);

    Page<Activity> result = activityRepository.findAll(
            buildSpecification(venueId, type, keyword), pageable);

    return PageResult.of(result, ActivityResponse::from);
}

private Specification<Activity> buildSpecification(Long venueId, ActivityType type, String keyword) {
    return (root, query, cb) -> {
        List<Predicate> predicates = new ArrayList<>();

        // 只查上架中的活动
        predicates.add(cb.isTrue(root.get("enabled")));

        if (venueId != null) {
            predicates.add(cb.equal(root.get("venue").get("id"), venueId));
        }
        if (type != null) {
            predicates.add(cb.equal(root.get("type"), type));
        }
        if (StringUtils.hasText(keyword)) {
            // 转小写后模糊匹配，避免大小写差异导致搜不到
            String pattern = "%" + keyword.trim().toLowerCase() + "%";
            predicates.add(cb.or(
                    cb.like(cb.lower(root.get("title")), pattern),
                    cb.like(cb.lower(root.get("description")), pattern),
                    cb.like(cb.lower(root.get("tags")), pattern)
            ));
        }

        return cb.and(predicates.toArray(new Predicate[0]));
    };
}
```

四个值得说明的点：

1. **`Math.min(size, maxPageSize)` 是必需的防御**。校验注解在 Controller 层限制了 `size <= 100`，但 Service 可能被其他入口调用（定时任务、内部 API），不能只靠 Controller 把关。
2. **排序加上 `id` 作为第二排序键**。只按 `createdAt` 排序时，若多条记录时间相同，分页可能出现“同一条记录出现在两页”或“某条被跳过”。
3. **`cb.lower()` 做大小写无关匹配**。搜 "spring" 能命中 "Spring Boot"。
4. **`readOnly = true`** 让 Hibernate 跳过脏检查快照，纯查询场景省内存和 CPU。

### 6.2 真实响应

```
GET /api/activities?page=0&size=2
```

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "items": [
      {
        "id": 5,
        "title": "Spring Boot 官方文档（online）",
        "description": "官方参考文档入口，版本随官方发布同步更新。",
        "venueId": 5,
        "venueName": "大学生活动中心",
        "type": "LINK",
        "typeLabel": "活动详情页",
        "url": "https://docs.spring.io/spring-boot/",
        "fileSizeText": "-",
        "approvedCount": 312,
        "organizer": "陶老师",
        "tags": "官方文档,参考",
        "createdAt": "2026-08-20 08:00:00",
        "updatedAt": "2026-08-20 08:00:00"
      },
      {
        "id": 4,
        "title": "接口调试工具包",
        "venueId": 4,
        "venueName": "体育馆",
        "type": "CODE",
        "typeLabel": "代码",
        "url": "/files/api-toolkit.zip",
        "fileSize": 10485760,
        "fileSizeText": "10.0 MB",
        "approvedCount": 45,
        "organizer": "教学组",
        "tags": "工具,调试",
        "createdAt": "2026-08-18 11:05:00",
        "updatedAt": "2026-08-18 11:05:00"
      }
    ],
    "page": 0,
    "size": 2,
    "total": 5,
    "totalPages": 3
  }
}
```

两条记录的结构**不完全一样**——第一条的 `description` 有值，第二条没有这个字段。这不是 bug，而是配置了 `default-property-inclusion: non_null`，值为 null 的字段不输出，响应体更干净。
